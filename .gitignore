# Inbox — transient dump zone, processed weekly, no value in tracking
00 Inbox/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Obsidian workspace state (open tabs, cursor position, etc.)
.obsidian/workspace.json
.obsidian/workspace-mobile.json

# Secrets — LinkedIn API credentials live here
00 System/scripts/linkedin/.env
00 System/scripts/linkedin/.tokens.json
__pycache__/
*.pyc

# LinkedIn image assets (can be large; not needed in git)
05 Content/LinkedIn/assets/